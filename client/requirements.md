## Packages
recharts | For displaying stock price trends and signal distribution
framer-motion | For smooth page transitions and micro-interactions
date-fns | For formatting dates and timestamps (e.g., "2 hours ago")
lucide-react | For beautiful iconography (already in base, but emphasizing usage)
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config: Extend font family to include 'Outfit' (display) and 'Inter' (body).
Auth: Uses Replit Auth via /api/login and /api/logout.
API: Strict adherence to @shared/routes.ts for data fetching.
